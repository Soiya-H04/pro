独立按键（Independent Keys）
包含头文件：REGX52.H。
寄存器：P3。


按键逻辑：一个 bit 位代表一个按键的按下与抬起（按键抬起时置 1；按键按下时置 0。）。

独立按键位置（P3）：
位寻址：
K1		K2 		K3		K4
P3_1	P3_0	P3_2	P3_3

按键防抖：
1. 问题 ：
当按下与抬起的瞬间，引脚会有一定的跳动，不能通过瞬时的电平来判断是否按下。
2. 解决：
在按下与抬起判断逻辑后加上一定延迟（Delay）可以防止跳动带来的判断失误。