
### Get-ChildItem (Alias: ls)

查找文件夹下所有文件与子文件夹

#### -file

使用 -file 参数，查找文件夹下所有文件

#### -Directory

使用 -Directory 参数，查找文件夹下所有文件夹

#### -Filter

使用 -Filter 参数，可以查找特定文本
使用`""`将文本包含起来，可以使用`*`代替多个字符



### Remove-Item

删除文件

### ForEach-Object

	使用这个能够对每个对象执行特定操作，操作用`{}`括起