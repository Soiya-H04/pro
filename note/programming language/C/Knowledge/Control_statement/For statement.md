#for #C #control_statement

## For 循环
---
### 一、概念

#### 1.1 三大条件

`for`语句需要三大条件：**初始化（总循环开始前执行）**、**循环条件判断（一次循环开始前判断）**、**改变循环条件（一次循环结束后执行）**
#### 1.2 循环条件

条件真值为真执行语句
#### 1.3 基本语法

使用`for`语句的基本语法为
```c
for (循环条件初始化; 循环条件判断; 改变循环条件)
```
#### 1.4 省略

三个条件都可以视条件省略

省略三个条件的语法为
```c
for (;;;)
```

